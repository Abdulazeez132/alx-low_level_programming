#include "main.h"

/**
 * _abs - prints absolute value of the num given
 *
 * @n: is the given num
 *
 * Return: returns 0
 */

int _abs(int n)
{

	if (n >= 0)
	{
		return (n);
	}

	return (n * -1);
}
