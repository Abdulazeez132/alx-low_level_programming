#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: 'Prints numbers from 0 to 10 with new line after'
 *
 * Return: Always 0'
 */

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		putchar((num % 10) + '0');
	/**
	 * lsdkdldjdssk
	 */
	}

	putchar('\n');

	return (0);
}
